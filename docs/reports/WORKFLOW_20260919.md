# 2026-09-19 实时参考工作流改进验证

此次改变调度、交互和组织结构；不宣称生成画质或身份一致性已经达标。

## 连续绘画

修改前：真实调度器 + 250ms 模拟推理、每 40ms 更新，24 次任务中 23 次取消，绘画期间 0 帧，停笔后 1 帧。

修改后：普通笔画仅淘汰待处理输入和 HQ，允许同一配置的近期预览完成；配置切换严格隔离，过期预览有 5 秒年龄上限。确定性回归用同步门让推理期间发生多次笔画更新，确认无需停笔仍能显示，并检查硬失效与超龄过滤。

真实 GPU 已预热测试 12 秒：256 次快照更新、11 张可见结果、无安全拦截；快照到轮询 P95 1.125 秒，最大显示间隔 1.125 秒。每 40ms 连续更新，未安排停笔。

[原始指标](../../artifacts/checks/continuous-gpu-20260919/metrics.json)记录逐帧时刻。此数值不包含鼠标派发和 Tk 上传，不保证每台机器的体验；也不是长时间稳定性证明。

## 自动与窗口回归

58 项测试通过，包括后台序列化、硬失效、持续笔画、超龄预览、HQ 取消、预设验证和失败恢复、保存的线稿与参数对应当前固定参考、暂停恢复、历史浏览，以及实际 Tk 控件缩放和橡皮擦。窗口测试使用模拟推理，不作为模型画质证明。

模拟综合压力 15 秒：[指标](../../artifacts/checks/stress-20260919/metrics.json)，572 次请求、21 张可见结果、245 次取消；模型操作最大并发 1，单后台线程，退出成功。故意损坏适配器产生的错误属于测试输入。

追加真实 Tk + GPU：[指标](../../artifacts/checks/gui-gpu-20260919/metrics.json)。在 RTX 4080 Laptop GPU 上预热后持续模拟落笔 12 秒，共 255 次输入、11 次实际 Tk 图片更新，最大更新间隔 1.297 秒，事件循环最大间隔 0.032 秒。固定参考后修改画布，保存像素和参数均仍对应固定图。隐藏窗口包含实际 PhotoImage/update，不包含硬件鼠标、显示器物理延迟；这是交互链路回归，不是长期压力或画质验收。

## GPU LoRA

[最终指标](../../artifacts/checks/runtime-20260919-final/metrics.json)验证：步骤取消后恢复、失败回滚后的适配器/权重/像素一致、同文件调权重不更换适配器、不同文件切换释放旧适配器、卸载后正常生成。

第一次 GPU 运行在旧的适配器名称断言处失败：原脚本假定相同文件也会重新加载，新实现会原地更新权重。已分成“同文件调权重”和“换文件”两项验收后通过，未通过的产物保留在 artifacts/checks/runtime-20260919。像素一致性断言在两次运行中均通过。

以上使用零增量 LoRA 测试接口。真实角色/画风尚未指定，未做身份一致性结论。后续按 [验收方案](../ACCEPTANCE.md) 补齐。

评估 CLI 的 `--profile` 也完成真实 GPU 冒烟：使用同一零增量适配器，触发词与自由描述组合后生成预览/精细各一张，无拦截；[指标](../../artifacts/checks/profile-evaluation-20260919/metrics.json)记录预设、实际提示词和权重。只验证配置传递与输出，不对这两张图作角色画质结论。
