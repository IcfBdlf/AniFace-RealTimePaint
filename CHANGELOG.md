# 工作日志

## 2026-09-21 — 核心源码逐函数学习手册

- 新增 docs/CODE_WALKTHROUGH.md，结合实际源码讲解 76 个核心函数、配置项、调用过程、线程及状态变化，并提供测试/工具阅读例子和学习练习。
- 更新文档导航；保留源码行号链接与文件哈希，明确学习摘录对应的代码版本。未修改应用逻辑。

## 2026-09-19 — 重写当前项目技术与面试指南

- 删除过时 PROJECT_GUIDE 与旧 README 备份；更新导航，避免旧实现混淆当前代码。
- 新增 docs/INTERVIEW_GUIDE.md：22 章技术与源码详解、设计取舍、测试证据、介绍模板及 25 个面试追问。第三方算法与项目自有实现分开描述，标明尚未验证的角色一致性。

## 2026-09-19 — 清理临时文件并归档产物

- 按用户明确要求删除安装缓存、已完成验证的独立临时环境、7 个过时一次性脚本和项目字节码缓存，释放约 6.835 GiB；当前 .venv、模型、素材和 Git 保留。
- artifacts 按 checks/evaluations/environment/model-cache/logs 分类；同步工具默认路径和文档链接，删除/移动清单存于 artifacts/environment。

## 2026-09-19 — 实时参考工作流与项目整理

- 明确产品目标：锁定角色/画风，由手绘控制并生成绘画参考。
- 普通落笔保留同配置近期预览，配置切换严格隔离；补充不停笔出图、过期过滤回归。
- 加入本地预设及成功后生效、LoRA 同文件调权重与卸载；真实角色一致性待用户指定模型验证。
- 默认手动精细重绘，加入暂停/固定参考/历史、橡皮擦/笔刷/缩放/平移；PNG 附带原请求线稿及参数。
- 源码、测试、工具、预设、文档分别归档；模型缓存和历史输出不搬动。旧入口映射见 docs/README.md。
- 58 项测试通过；GPU 不停笔 12 秒显示 11 帧、最大间隔 1.125 秒；GPU LoRA 回滚/调权重/切换/卸载通过，模拟综合压力 15 秒通过。详见 docs/reports/WORKFLOW_20260919.md。

## 2026-09-13 — 动漫专用 Lineart 困难样本验证

- 下载作者动漫Lineart至项目缓存；增加实验类型、独立控制缓存目录与clip_skip参数，GUI默认保持不变。
- 持剑图三种子两档，原编码和clip_skip=1共12次，9张可见已查看、3次拦截；仍有巨脸及线条装饰化，没有扩展到其他原稿。
- 51项测试通过；新增 ANIME_LINEART_REVIEW.md。下一步先检查各候选共同的基础模型/输入与推理设置，不将问题归因于尚未验证的单一原因。

## 2026-09-13 — 通用 Lineart 模型实验入口与对照

- 管线创建支持可选 lineart，输入处理绑定模型类型；默认 Scribble 不变。评估脚本新增 --control-type，记录类型和极性。
- 使用已有通用 Lineart 缓存完成24次真实线稿生成，19张可见输出已查看、5次拦截；全程出现浮雕纹理/错脸，短时丢失构图，因此未在GUI替换模型。
- 50项测试通过；新增 LINEART_REVIEW.md。动漫专用模型尚未下载或测试。

## 2026-09-12 — 可选尽量跟随线稿模式

- GUI 新增默认关闭的开关；控制结束比例随渲染请求传递，预览/精细一致，不修改提示词和全局配置。
- 模式参与请求版本和取消判断，同模式预览升级精细仍允许预览先显示。评估脚本支持 --follow-sketch。
- 48 项测试、隐藏窗口开关及线程传参验证通过；三张真实线稿×三个种子×两档共18次GPU输出已查看，双人人数保留改善，单人细线稿的巨脸/错位仍存在。

## 2026-09-12 — 用户真实线稿评估

- 使用阿福、cheng线稿、超跑T草稿，默认三种子18次及三个seed42对照组18次，共36次实际GPU生成；31张可见图已查看、5次拦截。
- 默认设置丢失全身构图和双人人数；延长控制改善部分轮廓但未解决持剑遮挡和脸部错位，中性提示词也未单独解决问题。
- 新增 REAL_SKETCH_REVIEW.md，更新交接记录；源图保持不变，应用默认参数未修改。

## 2026-09-12 — 惊讶提示词受控对照

- 三种候选、两类合成线稿、三个种子、两档，新增 36 次 GPU 生成，17 张可见图已查看、19 次拦截单独记录。
- 仅 surprised 可见结果减少；surprised, parted lips 表情较温和，但默认种子两类输入预览均拦截；眉眼描述候选多张偏向生气。
- 无候选同时满足目标，未替换应用预设。新增 SURPRISE_REVIEW.md，保留指标及原图，不宣称问题已修复。

## 2026-09-12 — 多种子表情评估与外部线稿入口

- evaluate_quality.py 支持重复指定种子、应用表情预设及外部线稿，矩阵共用一次模型加载，每组输出对比图，每条记录保存实际提示词、种子和表情。
- 外部线稿复用画板导入处理；新增无 GPU 测试覆盖组合数量、文件不冲突、预设应用和拦截记录，全套 45 项测试通过。
- 两类合成线稿 × 三种种子 × 四种预设 × 两档，完成 48 次 GPU 生成，31 张可见结果已查看，17 次被检查器拦截。惊讶出现夸张惊恐及画风漂移，闭眼微笑在种子 2026 出现镜框式构图；未改应用默认参数。
- 新增 EXPRESSION_REVIEW.md；项目无可用真实手绘线稿，未将合成样本冒充真实手绘。

## 2026-09-12 — 独立安装与连续操作验证

- 新建不继承系统第三方包的 artifacts/clean-env，完整安装依赖；33 个发行包均位于新环境，依赖检查及 44 项测试通过。
- 独立环境实际 GPU 取消、LoRA 回滚/替换和窗口生成、重试、关闭验证通过。README 补充先升级 pip，处理旧安装器解析 PyTorch 索引依赖失败。
- 新增 stress_renderer.py，检查连续更新、清空、LoRA 切换/故障下的旧帧过滤、模型串行和关闭，并保存内存等采样。
- 模拟 180 秒与 GPU 120 秒测试通过，分别提交 6899/252 次渲染请求。详见 ENVIRONMENT_STRESS_REPORT.md；未验证数小时运行或新机器安装。

## 2026-09-12 — 过时推理取消与 LoRA 切换回滚

- 渲染步骤回调同时检查关闭信号与请求版本；新输入、清空、LoRA 请求可在步骤边界结束过时推理。同一输入预览升级精细重绘不误取消。
- 新旧 LoRA 暂时共存，启用成功再删除旧适配器；加载或启用失败从内存恢复旧适配器和权重，不重新读取原文件。
- 恢复失败时暂停推理，重新加载时先清理模型状态；旧适配器释放失败单独提示并留待重试。
- LoRA 失败/恢复通知不再被随后成功的渲染帧清除。
- 44 项无 GPU 测试；新增 test_runtime_gpu.py 显式回归。实际 GPU 验证第 2 步取消、取消后继续推理、损坏 LoRA 回滚以及旧适配器释放均通过。使用零增量测试 LoRA，不代表所有社区风格兼容性。

## 2026-09-12 — 启动加载页、失败重试与关闭

- 窗口先显示启动页，模型在后台加载；提供阶段说明、活动进度条、错误详情与重试按钮。
- create_pipelines 支持进度回调及阶段边界取消，原有命令行评估调用方式兼容。
- 加载过程中关闭会等待当前调用结束；已排队的成功结果不会再进入画板。
- 后台不调用 Tkinter，也不主动触发全局垃圾回收；重试前的回收在主线程执行。
- 35 项测试通过。隐藏窗口验证失败重试、加载期间关闭、真实缓存模型加载后进入画板并生成图片；加载期间事件循环保持响应。

## 2026-09-12 — 表情预设与被拦截结果提示

- 增加不附加表情、微笑、闭眼微笑、惊讶预设。提交请求时组合提示词，切换不会污染用户输入，预览和精细重绘保持一致。
- render_sketch 根据 nsfw_content_detected 抛出 RenderBlocked；渲染器转成带版本号的 blocked 事件，过时通知同样被过滤。
- GUI 保留上一张有效图、说明本次结果被拦截；首张被拦截时不产生可保存结果，后续正常帧清除旧提示。
- 评估脚本记录 blocked 并生成带标注占位图；正常全黑像素不被擅自当作安全拦截。
- 无 GPU 测试扩展为 26 项并通过，覆盖预设切换、两档同步、检查标志、保留旧图、恢复和旧通知丢弃。
- 真实 GPU 粗糙线稿验证：预览报告 blocked，后续精细图正常；隐藏 Tk 窗口验证预设组合、保留有效图及恢复显示通过。

## 2026-09-12 — 多线稿画质评估

- 新增 evaluate_quality.py，四类合成线稿、预览/精细重绘对比，输出图片、总图及 JSON 指标，拒绝覆盖已有目录。
- 完成默认 0.35 控制比例、全程控制 1.0、闭眼表情提示词三组离线实验，共 18 张输出，包含两张检查器替换的黑图。
- 闭眼线条在默认提示词下未被正确还原；加 closed eyes, smiling 后两档呈现闭眼笑容；全程控制未解决表情问题。
- 新增 QUALITY_REVIEW.md 保存观察、限制与复现命令。未更改应用推理默认参数。

## 2026-09-11 — 统一 ControlNet 推理与渲染调度

- 用同一条 ControlNet 管线执行 10 步预览和 25 步精细重绘，固定随机种子，统一黑底白线输入。
- 修正 `control_guidance_end` 参数；移除未实际执行 ControlNet 的 StreamDiffusion 预览路径。
- 一个工作线程串行执行推理和 LoRA，图像队列只保留最新请求；主线程轮询结果并过滤旧版本。
- 清空/改提示词使旧结果失效；相同输入的预览可以在精细结果前显示。
- 增加停笔 800ms 重绘、异步 LoRA、状态/错误提示、PNG 保存、撤销/重做、等比例导入和坐标零值绘画。
- 关闭时停止接收新工作，在去噪步骤边界取消；等待后台退出后销毁窗口。
- 重写真实渲染器测试，新增推理契约和交互测试，共 17 项通过；GPU 基准复用应用管线。
- 隐藏 Tkinter 窗口生命周期检查通过：绘画、提示词更新、结果显示、清空、撤销/重做、安全关闭（使用真实渲染器和模拟推理）。
- 固定兼容依赖；项目内 `.venv` 的 `pip check` 通过，未修改原 conda 环境。
- RTX 4080 Laptop 实测预览 0.94–0.99 秒、精细重绘 2.13–2.15 秒（预热后各 3 次）；本地零增量 LoRA 的加载、权重设置和 GPU 推理验证通过。

---

## 2026-08-10 — P0 线程安全修复

### 背景

`canvas_stream.py` 是 Tkinter + Stable Diffusion ControlNet 实时线稿转动漫的交互画板。主线程处理 GUI 事件，后台线程执行模型推理。经审查发现多处线程安全隐患。

### 修复内容

#### 🔴 P0：消除竞态条件

| 问题 | 位置 | 修复 |
|------|------|------|
| `_preview_thread` 无锁访问 | `paint()` | 纳入 `state_lock` 保护 |
| `last_render_time` 无锁访问 | `paint()` | 纳入 `state_lock` 保护 |
| 后台线程读取 Tkinter `StringVar` | `async_hq_render()` | 主线程捕获 prompt 快照 → 存入 `_last_prompt` → 作为参数传入 |
| `sketch_img` / `draw_buffer` 并发读写 | 多处 | 新增 `canvas_lock` 统一保护 |
| `need_update_again` 处理存在 TOCTOU 窗口 | `async_hq_render()` | `is_rendering=False` + 清 flag + `is_rendering=True` 合并为一次原子操作 |
| MessageBox 在 `state_lock` 内调用 | `load_lora_weights_action()` | 条件检查在锁内 → 释放锁 → 再弹窗 |

#### 🔴 P0：优雅退出

- 新增 `_shutdown_event`（`threading.Event`）
- 新增 `on_closing()` — 设置退出事件，启动轮询
- 新增 `_check_shutdown_complete()` — 每 200ms 轮询，最多等 5 秒让 CUDA 线程安全结束
- 所有 `root.after()` 调用前检查退出信号，防止窗口销毁后崩溃

#### 🧪 测试

新增 `test_thread_safety.py`：17 项线程安全单元测试（mock pipeline，无需 GPU），全部通过。

### 文件变更

| 文件 | 变更 |
|------|------|
| `canvas_stream.py` | +116 / -70 行 |
| `test_thread_safety.py` | 新增 402 行 |
| `CHANGELOG.md` | 新增（本文件） |

### 提交

`2814fa1` — fix: P0 线程安全修复 — 消除竞态条件与优雅退出

---

## 2026-08-10 — 清理无关文件

- 删除 `transformer_test.py`：独立的 Transformer 模型实现，与 AniFace 画板项目无关

### 提交

`ded8c12` — docs: 新增工作日志 CHANGELOG.md  
`a2e0d48` — chore: 删除无关文件 transformer_test.py
